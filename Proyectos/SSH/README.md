# Exposición: Protocolo SSH

Trabajo expositivo sobre el protocolo SSH (capa de aplicación), presentado en la cursada de **Redes 2**.

- [`presentacion/`](presentacion) — el sitio de slides (Quarto + reveal.js): qué es SSH, el handshake paso a paso, métodos de autenticación y demos. Se compila con `quarto preview` / `quarto render` desde esa carpeta.
- [`demo-docker/`](demo-docker) — el entorno Docker (gateway SSH + Postgres) usado para las demos en vivo de la presentación.
